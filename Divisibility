#include <iostream>
#include<vector>
using namespace std; 
int main()
{     
	int num;     
	cin>>num;     
	vector<int> arr(num);    
	for(int i=0; i<num; i++)         
	cin>>arr[i];     
	int reminder = arr[num-1]%10;     
	if(reminder==0)         
	cout<<"Yes"<<endl;     
	else         
	cout<<"No"<<endl;     
	return 0; 
}
